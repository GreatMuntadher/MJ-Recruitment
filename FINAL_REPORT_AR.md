# التقرير النهائي — Dynamic Candidate Form

## حالة التنفيذ

- تم توحيد سؤال موعد المباشرة بعنوان **متى يمكنك المباشرة معنا؟** وبالمفتاح `available_start_date`.
- بقي الحقل التاريخي `current_available_date` موجوداً في `Form_Config` مع `Active = لا`.
- بقيت الأعمدة التاريخية في `Candidates`، وأضيف/اعتمد عمود `available_start_date` من دون تغيير معماري غير ضروري.
- نُشر Web App ويعمل على الرابط النهائي المدرج أدناه.

## الملفات

`Code.gs`، `ConfigService.gs`، `ValidationService.gs`، `CandidateService.gs`، `AcceptanceTests.gs`، `Index.html`، `Styles.html`، `Scripts.html`، `appsscript.json`، `test-results.json`.

الملفات المعيارية المنفصلة هي **المصدر الرسمي الوحيد للتطوير والنشر (Single Source of Truth)**. يجب أن يأخذ أي نشر جديد محتواه مباشرة من `Code.gs` و`ConfigService.gs` و`ValidationService.gs` و`CandidateService.gs` و`AcceptanceTests.gs` و`Index.html` و`Styles.html` و`Scripts.html` و`appsscript.json`. لا تُعتمد أي نسخة موحدة قديمة كمصدر تطوير أو نشر.

## الدوال الرئيسية

- `doGet`: تقديم واجهة Web App.
- `getFormConfig`, `readConfig_`, `parseConfig_`: قراءة `Form_Config` والتحقق منه وتحويله إلى تعريف ديناميكي للواجهة.
- `submitCandidate`: التحقق، القفل، منع التكرار، إنشاء الرقم، والحفظ.
- `validate_`: التحقق الخادمي من الحقول والخيارات والشروط والخبرات.
- `findSubmission_`: إعادة نتيجة الطلب السابق عند تكرار رمز الإرسال.
- `nextId_`: إنشاء رقم المرشح المتسلسل.
- `runAcceptanceTests`: تشغيل اختبارات القبول داخل Apps Script.

## القراءة والحفظ

تُقرأ أسماء الحقول والمفاتيح والأنواع والأقسام والترتيب والإجبارية والخيارات والمنطق الشرطي والمجموعات المتكررة والحد الأعلى من `Form_Config`. تُبنى الواجهة من هذا التعريف ولا تحتوي على قائمة حقول ثابتة داخل HTML.

يحفظ `submitCandidate` السجل الرئيسي في `Candidates` والخبرات في `Candidate_Experiences`، ويسجل العمليات والأخطاء في `System_Log`. تستخدم عملية الحفظ `LockService` وSheets Advanced Service، وتضيف أعمدة تخزين للحقول الجديدة عند الحاجة مع الحفاظ على الأعمدة التاريخية.

## Candidate ID ومنع الإرسال المكرر

تُنشأ الأرقام بالشكل `MJ-C-000001` داخل قفل Script Lock. يستخدم كل إرسال UUID ثابتاً كمفتاح idempotency؛ إعادة الإرسال بنفس المفتاح تعيد Candidate ID السابق ولا تضيف مرشحاً أو خبرات أخرى.

## الاختبارات

- اختبارات V1 المحلية: **26/26 PASS**.
- اختبارات القبول الحية داخل Apps Script: **13/13 PASS**.
- الحقول المطلوبة: نجح رفض النقص لكل حقل مطلوب.
- المنطق الشرطي: نجح الإظهار والإخفاء، وأصبحت الحقول التابعة مطلوبة عند تحقق الشرط فقط.
- الخبرات: نجحت حالات 0 و1 و3 و5، ورُفضت الخبرة السادسة والسجل الجزئي والتواريخ المعكوسة.
- Double submit: نجحت إعادة الرقم نفسه بلا تكرار للمرشح أو الخبرات.
- إضافة سؤال من الشيت دون تعديل HTML: PASS، مع إنشاء عمود التخزين عند الحفظ.
- تعطيل سؤال: PASS، اختفى من النموذج ولم يعد مقبولاً في الطلب.
- تغيير ترتيب سؤال: PASS، انعكس ترتيب `Form_Config` في الواجهة.
- تحويل Optional إلى Required: PASS، طبقه التحقق الخادمي فوراً.
- التحقق الحي من الواجهة المنشورة: 9 خطوات، RTL، صفحة مراجعة، سؤال موعد مباشرة واحد، منطق شرطي، وحد أقصى 5 خبرات. لم يُرسل طلب خلال فحص الواجهة.

اختبارات الحفظ الحية أنشأت السجلات التقنية `MJ-C-000001` إلى `MJ-C-000012`، وكلها موسومة في `Candidates` بأنها **اختبار تقني**.

هذه السجلات هي **QA/Test Data** وليست طلبات تشغيل حقيقية، ويجب استبعاد كل سجل يحمل حالة أو علامة اختبار من أي Dashboard أو تقرير تشغيلي مستقبلي. لا تُحذف أرقامها ولا تُعاد أرقام المرشحين الحالية.

## تحسينات V1 النهائية

- واجهة المرشح تستخدم الآن تسمية «بوابة التوظيف» و«استمارة بيانات مرشح» من دون وصف «داخلية».
- شاشة النجاح متمركزة داخل البطاقة، تعرض علامة SVG متحركة ورقم الطلب، وتزيل عناصر رأس البطاقة ومؤشر الخطوات بعد نجاح الإرسال، مع احترام `prefers-reduced-motion`.
- `Preview_Local.html` ملف مستقل لا يدخل في نشر Web App، ويستخدم محاكاة محلية لـ `getFormConfig()` و`submitCandidate()` من دون أي حفظ، ويعرض شارة «وضع المعاينة المحلية».
- بقي `available_start_date` الحقل الفعال الوحيد لموعد المباشرة. تظل الأعمدة التاريخية، ومنها `earliest_start_date` و`current_available_date`، محفوظة للتوافق السابق ولا تستخدمها الواجهة.
- إعداد `education_level` أصبح `select` بخيارات: ابتدائية، متوسطة، إعدادية، دبلوم، بكالوريوس، ماجستير، دكتوراه، أخرى.
- أصبح `erp_level` اختياريًا مع بقاء نوعه وخياراته وحالته الفعالة كما هي.
- أصبحت خيارات `marital_status`: أعزب، متزوج، مطلق، أرمل، منفصل.

## الروابط

- Web App: https://script.google.com/macros/s/AKfycby8t9J5SEcS5YtsPo8OK4Z-jopooj0_hdy_3_j9r0VoFQEADAMNOUY-x9KuR3WxVeTY/exec
- Google Sheet: https://docs.google.com/spreadsheets/d/1ovMm_MitkhZdsmvZZEntKXNlpDxEl0GYZJDl5Uvrnek/edit
